import { createStyles } from "@mantine/core";

const styles = createStyles(() => ({
  anim: {
    width: "100%",
    height: "100%",
    maxWidth: "20rem",
    margin: "auto",
    paddingTop: "3rem",
  },
  code: {
    "pre code": {},
  },
}));

export default styles;
