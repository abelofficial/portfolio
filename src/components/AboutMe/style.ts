import { createStyles } from "@mantine/core";

const styles = createStyles(() => ({
  container: {
    gap: "1rem",
    padding: "1rem",
    width: "fit-content",
    margin: "auto",
  },
  text: {
    textAlign: "justify",
    maxWidth: "30rem",
    margin: "auto",
    marginBottom: "1rem",
  },
}));

export default styles;
