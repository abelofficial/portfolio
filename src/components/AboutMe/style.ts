import { createStyles } from "@mantine/core";

const styles = createStyles(() => ({
  container: {
    gap: "1rem",
    padding: "1rem",
    width: "fit-content",
    margin: "auto",
  },
}));

export default styles;
